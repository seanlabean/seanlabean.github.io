Ronin calls for import and saturation

(clear)
(import $path)
(pixels fn ~q ~rect)

The Ronin call: (saturation pixel q) does something unknown. Maybe gets saturation quantity at pixel? Changes sat of one pixel
